{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Feed list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Body</th>
                <th>Title</th>
                <th>Image</th>
                <th>Source</th>
                <th>Publisher</th>
                <th>Created</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('feed_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.body }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.image }}</td>
                <td>{{ entity.source }}</td>
                <td>{{ entity.publisher }}</td>
                <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('feed_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('feed_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('feed_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
